server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/MenyManagment?createDatabaseIfNotExist=true
    username: root
    password: Awantha65@

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

